# CASE_STUDIES — Aetheria 2099 엔지니어링 케이스 스터디

> 각 사례: **문제 → 접근 → 결과**. "무엇을 만들었나"가 아니라 "어떤 판단을 왜 했나"를 남긴다.

---

## 1. 소형 모델이 JSON을 안 깨게 만든 법 — 출력 3중 방어선

**문제.** 게임은 매 턴 AI가 만든 JSON(나레이션·NPC 대사·선택지·수치 변화)에 의존한다.
클라우드 모델은 대체로 지키지만, 스팀 배포용 **로컬 7B**는 형식이 자주 무너진다 — 마크다운
펜스, 한자/중국어 문장부호 누출, 문자열 안에 JSON 구조가 새어들고, 같은 단어를 반복한다.
한 번의 붕괴가 게임 진행을 끊는다.

**접근.** 모델을 믿지 않고 **구조로 방어**했다.
1. `responseSchema` — Ollama 구조화 출력/스키마로 디코딩을 제약해 유효 JSON을 강제.
2. `safeParse` — 그래도 깨지면 관대하게 파싱(첫 `{`~마지막 `}` 슬라이스).
3. `normalize` — 최후 방어선: enum 폴백, 수치 클램프, **한자/CJK 문장부호 제거**, 선택지
   길이컷, **JSON 조각 누출 제거**(`","key":…` 꼬리 절단), 반복 garble 교정(`저스로→스스로`),
   그리고 **불법 노드 점프 차단**(현재 노드의 허용 후속으로만).

**결과.** 홀드아웃 40개 준수율 **93.2%**(로컬 v3). 붕괴가 나도 게임은 안 끊긴다. 이 방어선
로직은 순수 함수로 분리돼 **유닛 테스트 77개**로 회귀를 막는다. 교훈: *모델 품질을 못 믿는
환경에선, 품질을 "구조"로 사후 보장하라.*

---

## 2. 이중 양자화 미스터리 — 로컬 모델이 왜 7점 낮았나

**문제.** 맥에서 로컬 파인튜닝한 7B(v2)가 준수율 **81%**로, 예전 Colab 학습본(88.6%)보다
7.6점 낮았다. 학습 자체는 정상(loss 잘 수렴)이었고 데이터도 깨끗했다. "MLX가 약해서"로
치부하기 전에 진짜 원인을 파야 했다.

**접근.** 두 파이프라인의 차이를 분해했다. 결정적 차이는 **어떤 가중치에 LoRA를 병합했는가**:
- Colab(Unsloth): **진짜 fp16 원본**에 병합 → q4_K_M 양자화 **1회**.
- 내 MLX 경로: **이미 4bit인 베이스**에 학습 → dequantize(4bit 오차가 박힌 fp16) →
  다시 q4_K_M → **양자화 2회, 손실 누적**.

LoRA 어댑터는 정상이므로 **재학습 없이** 어댑터를 fp16 원본에 재병합하면 된다(QLoRA 정석).

**결과.** fp16 원본(bf16)을 받아 v2 어댑터를 재병합 → **81% → 88.6%로 7.6점 완전 회복**,
구 모델과 동률. 진단이 옳았음을 수치로 증명. 교훈: *"도구가 약하다"는 결론 전에, 파이프라인
단계별 손실을 분해하라. 종종 범인은 모델이 아니라 배관이다.*

---

## 3. 턴 간 반복을 데이터로 잡다 — 단일턴에서 멀티턴 증류로

**문제.** 로컬 모델이 대화 중 **직전 자기 대사를 반복**했다(예: NEXUS가 "나는 너희를
지켰단다"를 턴마다 되풀이). 추론 시점 패치(`repeat_penalty`)로는 한계 — 1.3까지 올리면
문장이 단어 샐러드로 붕괴했고(1.15가 타협선), 그마저 근본 해결이 아니었다.

**접근.** 근본 원인을 짚었다: **학습쌍이 전부 `(단일 입력 → 응답)`이라, 모델은 "직전 대사를
보고 다르게 말하는 법"을 한 번도 배운 적이 없다.** 그래서 교사(Claude 서브에이전트)가 각
장면을 **실제 게임처럼 K턴 이어 플레이**한 대화를 생성하고, 게임 루프(`applyResponse`)로
재생해 **직전 턴 문맥(recent_turns)이 누적된** 학습쌍을 만들었다(`make-conversations` →
교사 재생 → `assemble-conversations`). API 비용 0(교사=플랜 세션 서브에이전트).

**결과.** 멀티턴 데이터를 섞어 학습한 v3에서 **축자 반복 소멸**(인게임 4턴 검증), 준수율도
88.6→**93.2%**. 교훈: *추론 패치는 증상 완화. 실패 모드가 "학습된 적 없는 능력"이면, 그
능력을 데이터로 가르쳐라.*

> 후속(v4): 멀티턴 데이터를 2배로 늘렸으나 에폭을 줄여(2→1.6) 90.4%로 오히려 하락 →
> **데이터를 늘리면 에폭도 유지/증가해야 한다**는 교훈. v3 유지.

---

## 4. 자동 플레이테스트로 밸런스 결함을 찾다

**문제.** "여러 번 돌려 다듬은 완성도"를 주장하려면 실제 플레이 데이터가 필요했다. 수작업
완주는 로컬 모델 지연(턴당 ~15초) 때문에 반복이 비싸다.

**접근.** Playwright로 **5개 전략(정직·공격·은신·거래·무작위)이 전 루트를 자동 완주**하는
하네스를 짰다(`scripts/playtest.mjs`). 각 런의 턴 수·도달 엔딩·게이지 극단값·실패 여부를 수집.

**결과(발견).**
- 🔴 **세력 엔딩 도달 불가** — 호감이 시작값(10)에서 안 오름(모델이 affinity를 거의 0으로
  출력) → 세력 엔딩(문턱 40) 못 열림 → 절반이 SOLO_EXIT로 수렴. 게임의 중심 축이 보상으로
  안 이어짐.
- 🔴 **엔딩 게이트 미강제 버그** — `next`가 6개 엔딩을 다 나열해 결정론적 게이트가 우회됨
  (조각 1개로 진엔딩 도달).
- 🟠 **추적(heat) 무력** — 급습·체포가 0회.

**조정.** heat baseline ↑, 게이트 강제, **호감 바닥 보장**(세력과 우호적이면 모델과 무관하게
매 턴 최소 +5 — 곱셈 레버가 "0에 곱하기"라 무효였던 걸 대체). → 재플레이테스트로 검증.
교훈: *밸런스는 직관이 아니라 로그로. 그리고 잘못된 레버(0을 증폭)를 당겼음을 데이터가 알려준다.*

---

## 5. Colab을 버리고 맥에서 — 끊기지 않는 로컬 학습 인프라

**문제.** 무료 Colab(T4)이 **런타임 끊김·Drive 저장 실패로 학습 결과를 두 번 유실**.
"저장 완료" 로그가 거짓 양성이었다(파일 시각으로 판별).

**접근.** Apple M5(24GB)에서 **MLX 로컬 학습**으로 전환. 세 가지 함정을 넘었다:
- `huggingface_hub` 다운로더가 대형 파일에서 0 B/s로 멈춤 → **구간 seek·재시도 파이썬
  다운로더**(`dl_shard.py`)로 우회. 원격 Content-Length로 정확 크기 검증.
- batch↑ + grad-checkpoint 제거 → **24GB 초과로 시스템 다운**(실측) → **batch 2 +
  grad_checkpoint**(Peak ~11GB)가 안전선. `caffeinate`로 잠자기 방지.
- Ollama가 삭제 모델의 블롭을 GC 안 해 **61GB 방치** → manifest 참조 기반 orphan 정리.

**결과.** 끊김·비용 0의 재현 가능한 로컬 학습 파이프라인. 스팀/데스크톱의 오프라인·운영비 0
구동(번들 로컬 모델)의 토대. 교훈: *무료 인프라의 숨은 비용은 신뢰성. 통제 가능한 로컬이
"여러 번 돌리는" 반복 개발엔 더 싸다.*
