# ✍️ 작가용 편집 가이드 — Aetheria 2099

> 목적: 이 작품을 **당신의 창작물**로 만들기 위한 문서. 어디를 바꾸면
> "창작"이 되고, 어디를 건드리면 게임이 깨지는지 명확히 구분한다.
> **코드 지식 없이도** 텍스트만 골라 고칠 수 있게 썼다.

---

## 0. 먼저 알아야 할 핵심 원리

이 게임의 대사는 **두 종류**다:

1. **고정 텍스트** — 당신이 파일에 직접 쓴 문장. 그대로 출력됨.
   → 프롤로그 오프닝, 기억 조각(코덱스) 문구.
2. **AI 생성 텍스트** — Gemini가 매 턴 즉석에서 만드는 대사.
   → NPC 대사, 선택지 대부분.

**중요:** AI 대사는 파일에 없다. 대신 **"화법 가이드(npcVoice)"와 "지문
(setting)"으로 AI의 문체를 조종**한다. 즉 당신의 목소리는 두 경로로 들어간다:
- 고정 텍스트를 직접 쓴다 (완전한 통제)
- AI에게 주는 지시문의 톤을 바꿔 AI가 당신 스타일을 흉내내게 한다 (간접 통제)

이 가이드는 두 경로 모두를 다룬다.

---

## 1. 편집 대상 파일 지도

| 파일 | 무엇이 들었나 | 당신이 만질 곳 | 난이도 |
|---|---|---|---|
| **`src/game/voices.js`** | **4명의 말투 + 예시 대사** | `NPC_VOICES` 전체 | ⭐가장 쉬움·효과 큼 |
| `src/game/lore.js` | 세계관 설정 + 프롤로그 오프닝 | `WORLD_LORE`, `OPENING` | 쉬움 |
| `src/game/scenes.js` | 14개 씬의 지문·목표·딜레마·조각 | `SCENES` 안의 텍스트 필드 | 보통 |

**여기부터 시작:** `voices.js` 하나만 채워도 게임 전체 대사 문체가 당신 것으로
바뀐다. 세 파일 모두 **문자열(따옴표 안 글자)만** 바꾸면 된다.

---

## 2. 🟢 마음껏 다시 써도 되는 것 (= 당신의 창작)

### (A) 세계관 톤 — `lore.js`의 `WORLD_LORE`
백틱(`` ` ``) 사이의 설정 텍스트. 지금은 영어 초안이다.
- **당신 문체로, 한국어로 다시 써도 된다.**
- `[SETTING]` `[SECTORS]` 같은 대괄호 라벨은 **유지**하되(AI가 구획을 읽는 표지),
  그 아래 설명 문장은 자유롭게 각색.
- 세계관의 "사실"(넥서스=설계자 의식, 칩#00=외부 정화 영상 등)은 유지하되
  **묘사·분위기·단어 선택은 전부 당신 것으로.**

### (B) 프롤로그 오프닝 — `lore.js`의 `OPENING`
게임 첫 화면에 **그대로** 뜨는 문장. 완전한 통제 구간.
```js
npc_response: '빗물이 네온을 타고 흘러내린다. …',   // ← 이 문장을 당신 글로
generated_choices: [
  { text: '[솔직하게] 칩을 주머니에 넣고 …', tone: 'Honest' },     // ← text만 교체
  { text: '[거짓말] 아무것도 못 받은 척 …',  tone: 'Deceptive' },
  { text: '[위협/도발] 배달원의 멱살을 …',   tone: 'Aggressive' },
]
```
- `npc_response`, 각 선택지의 `text` → **자유롭게 리라이팅.**
- `tone` 값(`Honest`/`Deceptive`/`Aggressive`)은 **그대로 둔다**(§3 참조).

### (C) 각 씬의 창작 필드 — `scenes.js`의 `SCENES`
씬 하나(`ACT1_REN_GARAGE_01` 등)마다 아래 필드가 있다. **전부 당신 글로 교체 가능:**

| 필드 | 뜻 | 리라이팅 팁 |
|---|---|---|
| `title` | 씬 제목(화면 하단 표시) | 시적으로. 예: "값을 치르는 법" |
| `setting` | 배경 지문 (AI가 묘사의 재료로 씀) | 오감·분위기를 당신 문체로. 여기가 톤을 제일 좌우함 |
| `goal` | 이 장면의 극적 목표 | 무엇을 느끼게 할지. 문장 짧게 |
| `beats` | AI가 짚어야 할 전개 포인트 | 순서대로. "무엇이 일어나야 하는가" |
| `npcVoice` | **그 NPC의 말투 지시** | ★가장 중요★ (§4 참조) |
| `dilemma` | 강제되는 선택의 무게 | "A할 것인가 B할 것인가" 형태 |
| `revealsFragment` | 해금되는 기억 조각 문구 | 코덱스에 **그대로** 뜸. 명대사로 |

---

## 3. 🔴 절대 바꾸면 안 되는 것 (= 게임이 깨짐)

아래는 **코드가 정확히 일치해야 작동**한다. 오타 하나면 멈춘다.

1. **노드 ID** — `ACT1_REN_GARAGE_01` 같은 대문자_영문 키.
   - `lore.js`의 `STORY_NODES` 목록과 `scenes.js`의 키가 **철자까지 동일**해야 함.
   - 새 씬을 추가하려면 §5 참고(두 곳 다 등록).
2. **enum 값(정해진 단어들)** — 아래 단어는 **철자 그대로**:
   - 감정: `Neutral` `Suspicious` `Friendly` `Threatening`
   - 배경톤: `Normal` `Danger` `Melancholy` `Forest_Glitch`
   - 선택지 톤: `Honest` `Deceptive` `Aggressive`
   - NPC 이름: `Ren` `Kael` `Echo` `NEXUS`
   - → 이 단어들의 **화면 표시명은 UI에서 따로 바꿀 수 있다**(§6).
3. **구조 필드** — `next`(다음 노드 목록), `beatBudget`(페이싱 숫자),
   `gate`의 **노드 ID 부분**. 이건 이야기의 뼈대·분기·속도라 건드리면 흐름이 깨진다.
   - 단, `gate`의 **조건 설명 텍스트**(`<= ` 뒤 한국어)는 다듬어도 된다.
4. **따옴표·쉼표·중괄호** 같은 문장부호 구조. 글자만 바꾸고 부호는 그대로.

> 안전 규칙: **작은따옴표 `'…'` 안의 글자만 바꾼다.** 따옴표 밖(필드 이름, 대문자
> ID, 숫자)은 건드리지 않는다. 이 규칙만 지키면 절대 안 깨진다.

---

## 4. ★★ 가장 쉽고 강력한 편집 — `voices.js`

AI 대사는 파일에 없으므로, **말투를 지시문으로 규정**한다. 그 규정을 한 곳에
모아둔 파일이 `src/game/voices.js`다. 여기만 채우면 4명 전부가 당신 목소리로 말한다.

각 캐릭터는 두 칸으로 되어 있다:
```js
Ren: {
  style: '짧고 건조한 문장. 비유에 돈·부품·이자를 섞는다. …',   // ← 말투 지시
  examples: [                                                    // ← 당신이 쓴 대표 대사
    '공짜는 없어. 넌 뭘 담보로 걸 건데?',
    '정보도 부품이야. 값을 치르면 돌아가지.',
  ],
},
```

**작성 요령:**
- `style`: 추상어("냉정한") 대신 **구체적 지시**("마침표로 끊어라, 형용사를 아껴라").
  자주 쓰는 **단어군**("렌은 값·이자·부품 비유")과 **금지 표현**("에코는 '아마도'
  안 씀")까지 지정하면 좋다.
- `examples`: **당신이 직접 쓴 대표 대사 2~3줄.** 이게 문체 견본이 되어 AI가 모방한다.
  → **예시 대사 1~2개가 형용사 열 개보다 강력하다.** 여기에 당신 글솜씨를 쏟아라.

> 특정 씬에서만 말투를 살짝 비틀고 싶으면, `scenes.js`의 해당 씬에 `npcVoice`
> 한 줄을 더하면 그 씬에서만 덧입혀진다(전역 목소리는 voices.js가 담당).

---

## 5. 새 씬을 추가하고 싶다면 (선택)

두 곳에 등록해야 한다:
1. `lore.js`의 `STORY_NODES` 배열에 새 ID 한 줄 추가.
2. `scenes.js`의 `SCENES`에 같은 ID로 씬 블록 추가(기존 것 복사해 텍스트 교체).
3. 이어지는 씬의 `next` 배열에 새 ID를 넣어 연결.

ID 규칙: `ACT2_ECHO_BROADCAST_03`처럼 `막_인물_장소_번호` 대문자.
막히면 나(Claude)에게 "이 씬 추가해줘" 하면 등록까지 처리해준다.

---

## 6. 이름 표시 바꾸기 (선택)

enum 원본 단어(`Ren`, `Danger` 등)는 못 바꾸지만, **화면에 보이는 한글 라벨**은
바꿀 수 있다:
- NPC 한글명·색: `src/components/StatusPanel.jsx`의 `NPC_COLOR`
- 배경톤 한글명: 같은 파일 `TONE_LABEL`
- 엔딩 제목·경구: `scenes.js`의 엔딩 `title` + `EndingScreen.jsx`의 `epigraph`

---

## 7. 바꾼 뒤 확인하는 법

편집 후 터미널에서:

```bash
npm run build
```

- **성공** → 텍스트 반영 완료. `npm run dev`로 플레이 확인.
- **에러** → 십중팔구 §3의 따옴표/쉼표를 건드린 것. 직전 수정을 되돌리면 된다.
  (Git을 쓴다면 `git diff`로 무엇을 바꿨는지 바로 보인다.)

---

## 8. 추천 작업 순서

1. **`voices.js`** 4명의 `examples`에 **당신이 쓴 대표 대사**를 채운다 → 게임 전체
   대사가 당신 목소리로 바뀐다. (가장 쉽고 효과 큼 — 여기부터)
2. `OPENING`(첫 화면)을 당신 최고의 문장으로 → 첫인상이 곧 포트폴리오 인상.
3. `WORLD_LORE`를 당신 문체·한국어로 다시 쓴다 → 작품의 뼈대가 당신 것이 된다.
4. 각 씬의 `revealsFragment`를 **명대사**로 다듬는다 → 코덱스가 곧 하이라이트 릴.
5. 나머지 `setting`/`goal`/`beats`는 여유될 때 각색.

> 이 다섯 단계만 거치면, "AI가 만든 게임"이 아니라 **"AI를 붓으로 쓴 당신의 작품"**
> 이 된다. 구조(엔진)는 공학, 텍스트(영혼)는 당신 것 — 이 분리가 포트폴리오의 핵심.
