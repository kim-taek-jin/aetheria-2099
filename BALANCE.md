# BALANCE.md — 자동 플레이테스트 밸런스 리포트

> 방법: Playwright로 5개 전략(정직·공격·은신·거래·무작위)이 로컬 모델 v3로 전 루트를
> 자동 완주. 각 런의 턴 수·도달 엔딩·게이지 극단값·실패 여부를 수집. (`scripts/playtest.mjs`)

## 원시 결과 (2026-08-21)

| 전략 | 결과 | 턴 | heat 최대 | 의심 최대 | 호감 최대 | 조각 |
|---|---|---|---|---|---|---|
| 정직·신뢰 | SOLO_EXIT | 13 | 0 | 39 | 10 | 0 |
| 공격·폭로 | JAYNE_ORIGIN* | 13 | 20 | 20 | 10 | 1 |
| 은신·잠행 | SOLO_EXIT | 11 | 0 | 20 | 10 | 0 |
| 거래·실리 | SOLO_EXIT | 17 | 10 | 57 | 5 | 0 |
| 무작위 | NEXUS_TRUST | 13 | 2 | 29 | 10 | 2 |

- 엔딩 분포: SOLO_EXIT ×3, JAYNE_ORIGIN ×1, NEXUS_TRUST ×1
- 실패(체포/급습): **0회 / 5런**
- 페이싱: 11~17턴 (설계 목표 ~14턴과 부합 ✅)

## 발견된 문제 (심각도순)

### 🔴 P1 — 세력 엔딩 도달 불가 (호감이 안 쌓임)
5런 전부 **최고 호감이 시작값(10)에서 안 올라감**. 세력 엔딩(Ren/Kael/Echo)은 `maxAffinity≥40`
필요 → **정상 플레이로는 열 수 없다.** 그래서 절반 이상이 SOLO_EXIT(안전망)로 수렴.
- 원인 추정: 모델의 `affinity_change`가 대부분 0~소폭. 초반/종반은 NEXUS 무대라 세력 호감이
  움직이는 턴이 Act1~2의 4~6턴뿐인데, 그마저 증가폭이 미미.
- **핵심 결함** — "세력과 연대한다"는 게임의 중심 축이 보상으로 이어지지 않는다.

### 🔴 P1 — 엔딩 게이트가 실제로 강제되지 않음 (버그)
공격 런이 **조각 1개**(진엔딩은 빈자리 4개 필요)로 `ENDING_JAYNE_ORIGIN`에 도달.
원인: `scenes.js`의 `ACT3_DESIGNER_CONFRONT_01.next`가 6개 엔딩을 전부 나열 →
`validBranch`의 `allowed = [cur, ...next]`에 모든 엔딩이 포함 → `eligibleEndings` 게이트가
우회됨. 결정론적 게이트(설계의 핵심)가 무력.

### 🟠 P2 — 추적(heat)·위험이 무력
heat 최대 0~20, 드론 급습(100)·체포가 5런 내내 0회. NEXUS 추적이라는 긴장 장치가
실현되지 않음. 눈에 띄는 행동(해킹·폭로)도 heat를 충분히 올리지 못함.

### 🟡 P3 — 증거/조각 과소 수집
조각 0~2개. 증거 제시 메커닉과 빈자리(진엔딩) 경로가 거의 안 쓰임.

## 조정 제안 (수치)

> 재학습 불필요 — 전부 클라이언트 로직(`state.js`/`scenes.js`) 튜닝. 적용 후 재-플레이테스트로 검증.

1. **호감 가속 (P1)** — `applyResponse`에서 무대 NPC가 세력이고 `affinity_change>0`이면
   증폭(예: ×1.8 또는 +2 보너스). 목표: 한 세력에 꾸준히 우호적이면 ~5턴에 40 도달.
   대안/병행: 세력 엔딩 문턱 40→**30**, 신뢰 엔딩 60→**45**로 하향.
2. **게이트 강제 (P1)** — `validBranch`에서 `endingChoiceNode`일 때
   `allowed = [cur, ...eligibleEndings(save)]`로 제한(‌`scene.next` 대신). 단, **1번을 먼저**
   적용해 세력 엔딩이 실제로 열리게 한 뒤 강제할 것(아니면 전부 SOLO_EXIT로 붕괴).
3. **추적 강화 (P2)** — 눈에 띄는 행동의 heat baseline +2→**+4**, 잠행 감소 −2 유지.
   heat 경고(70)·급습(100)이 공격적 플레이에서 실제로 위협이 되게.
4. **조각 유도 (P3)** — 씬 앵커에 증거·빈자리 획득 훅을 더 자주 노출(프롬프트), 또는
   특정 노드 통과 시 조각 자동 부여.

## 해결 (2026-08-22)

**근본 원인은 밸런스 수치가 아니었다.** floor·문턱·게이트를 아무리 조정해도 호감이 시작값에
고정됐는데, 인게임 `recentTurns`를 확인하니 **모델이 씬의 화자를 매 턴 NEXUS로 잘못 귀속**
하고 있었다(Ren 정비소에서도 npc_name=NEXUS) → 세력 호감이 **아예 안 건드려짐**.

**진짜 수정:** `normalize`에서 **npc_name을 씬 바이블의 STAGE_NPC로 강제**(소형 모델이 화자를
못 정하는 걸 씬 정보로 교정). 이후 floor·문턱·게이트가 실제로 작동.

### 적용된 변경
1. **화자 강제** — `npc_name = SCENES[currentNode].npc` (근본 수정).
2. **호감 바닥 보장** — 세력과 비공격 상호작용 시 매 턴 최소 +6(`AFFINITY_FLOOR`), 모델 음수도 무시.
3. **엔딩 문턱 하향** — 세력 40→**20**, 신뢰 60→50.
4. **게이트 강제** — 엔딩 선택 노드는 `eligibleEndings`만 허용(무자격 엔딩 차단).
5. **MAX_TURNS 안전장치** — 엔딩 노드 예산 초과 시 자격 엔딩으로 강제 종료.
6. **추적 강화** — 눈에 띄는 행동 heat baseline +2→+4.

### 재검증 결과 (전 → 후)
| 지표 | 전 | 후 |
|---|---|---|
| 엔딩 분포 | SOLO×3, JAYNE×1, NEXUS×1 | REN×2, ECHO×1, KAEL×1, SOLO×1 |
| 최고 호감 | 5~10 (고정) | 29~52 (축적) |
| 세력 엔딩 | 0 | **3종 모두 등장** |
| MAX_TURNS / 실패 | — | 0 / 0 |

**남은 미세과제(P3):** heat 최대 50 — 급습(100) 발동엔 아직 부족(공격 극단 플레이 추가 강화
여지). 조각 수집 유도. Ren 소폭 과대표(Act1이 항상 렌 씬) — 필요 시 미세조정.
