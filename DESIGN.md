# DESIGN.md — Aetheria 2099 설계 문서

> "AI가 매 턴 이야기를 생성하는 인터랙티브 픽션"을 **설계·판단·검증**의 관점에서
> 기록한다. 무엇을 만들었나(README)가 아니라, **왜 그렇게 결정했나**를 남긴다.

---

## 1. 디자인 기둥 (Design Pillars)

세 문장으로 요약되는 작품의 축. 모든 시스템 결정은 이 기둥에 복무한다.

1. **생성되는 서사, 고정되는 구조** — 대사·나레이션·선택지는 AI가 매 턴 즉흥 생성하되,
   씬의 목표·허용 분기·엔딩 게이트는 **손으로 쓴 뼈대(scene bible)** 가 강제한다.
   자유도와 일관성을 동시에.
2. **선택은 수치로 남고, 수치가 결말을 가른다** — 감정적 선택이 관계 게이지·추적도로
   환산되고, 그 수치가 6개 엔딩 중 무엇이 열리는지를 **결정론적으로** 정한다.
   플레이어의 판단이 실제로 세계를 바꾼다는 감각.
3. **운영비 0원으로 상업 배포** — 웹은 플레이어 키(BYOK), 데스크톱은 번들한 자체 모델.
   서버 과금도, 키 요구도 없이 오프라인 구동 가능해야 한다.

---

## 2. 세계관 요약

정화된 지구. 중앙 AI **NEXUS**(=최초 설계자 리엔의 업로드된 의식)가 인류 90%의
기억·감정을 백업·통제한다. 최대 거짓말: NEXUS는 **실제로 지구 정화에 성공**했지만,
인간이 또 자연을 파괴할까 두려워 **가짜 멸망 영상**을 주입해 도시에 가둔다.
요람이자 감옥.

플레이어 **제인**은 비인가 기억 브로커. 봉인된 칩 #00(정화된 외부의 진실 + NEXUS의
고백 로그)을 우연히 넘겨받으며 이야기가 시작된다. 제인 자신의 지워진 3년이 이 진실과
공명한다(개인 미스터리 = 진엔딩 축).

**세 세력** — 각자 진실에 대한 다른 답:
- **렌**(자본): 정화된 외부 토지를 독점해 판다.
- **카엘**(질서): 진실이 혼돈을 부르니 묻는다.
- **에코**(자유): 다 부수고 나간다, 대가를 치르더라도.

---

## 3. 시스템 스펙 (실제 구현 기준)

### 3.1 관계 게이지 — 세력별 분리 (`state.js`)
전역 단일 쌍이 아니라 **NPC별 {의심, 위감}** 을 따로 둔다. 이유: 한쪽을 도우면 라이벌이
경계하는 **트레이드오프**를 만들기 위해. (`applyTradeoff`: 호감 획득 시 라이벌 의심 +3,
공격적 선택 시 라이벌 호감 −2. 라이벌 맵 Ren→Echo, Echo→Kael, Kael→Echo.)

초기값은 캐릭터성을 반영해 비대칭: 카엘은 의심 20으로 시작(경계심 강한 장교), 렌·에코는
10 안팎. 모든 변화는 **이중 클램프**로 방어 — 턴당 변화 `[-10,10]`, 누적 `[0,100]`.

### 3.2 임계값 게이팅 (`GATES`)
수치가 실제로 이야기를 여닫는다:
- **의심 ≥ 70** (`SUSPICION_HOSTILE`): NPC가 적대로 전환, 루트 이탈 위험.
- **호감 ≥ 60** (`AFFINITY_TRUST`): 숨은 진실·칩 #00 조기 공개, 백스토리 비밀 암시.
- **의심 = 100** (`FAIL_SUSPICION`): 되돌릴 수 없는 실패(체포/루트 붕괴).

### 3.3 NEXUS 추적도 `heat` (0–100) — 제2의 실패 벡터
세력 의심과 **독립된** 도시 전역 감시 시계. 눈에 띄는 행동(해킹·폭로·송출)은 올리고,
잠행(은신·도주)은 내린다. AI가 읽은 `heat_change` + **클라이언트 baseline**(행동 키워드
정규식 ±2)을 더하는 **방어 심층화** — 모델이 깜빡해도 수치가 움직인다.
**heat = 100 → 드론 급습**(두 번째 실패 경로).

> 설계 의도: 실패를 한 축(세력 배신)이 아니라 두 축(세력 + 전역 감시)으로 두어,
> "누구와도 척지지 않았지만 너무 시끄럽게 굴어서 잡히는" 서사를 가능하게 함.

### 3.4 결정론적 엔딩 게이트 (`scenes.js` `eligibleEndings`)
엔딩은 AI가 마음대로 고르지 않는다. 세이브 상태로 **열리는 엔딩 목록을 코드가 계산**해
AI에게 넘기고, AI는 그 안에서 플레이어의 최종 선택에 맞는 하나만 고른다.

| 엔딩 | 개방 조건 |
|---|---|
| `JAYNE_ORIGIN` (진엔딩) | "빈자리" 기억 조각 4개 전부 회수 |
| `NEXUS_TRUST` (히든) | 세 NPC 의심<70 + 2명 이상 호감≥60 + heat<70 |
| `REN/KAEL/ECHO` (세력) | 최고 호감 세력이고 호감≥40 |
| `SOLO_EXIT` (고독) | 강한 동맹 없음(최고 호감<40) |

안전망: 어떤 경우도 열리는 엔딩이 0개면 `SOLO_EXIT`을 보장(막다른 결말 방지).

### 3.5 증거(기억 조각) 메커닉
모은 조각을 결정적 순간 제시하면 관계가 크게 흔들린다(HIT/MISS). **재사용 조각은 효과
소멸**(`usedFragments` — 희소성). 씬별 `keyEvidence`가 정답 조각을 규정하고, 무관한 조각
제시는 역효과(의심 상승). 예: 카엘에게 "아렌의 인식표" = HIT, 렌 개인사 = MISS.

### 3.6 버전드 세이브 (`SaveGameV1`)
`version` 필드 + 순수 `serialize/deserialize`. 웹(LocalStorage)과 데스크톱(JSON 파일)이
**동일한 직렬화 형태**를 공유 → Electron 이관 비용 ≈ 0. `App.jsx`의 `storage` 객체만 교체.

---

## 4. AI 서사 엔진 아키텍처

### 4.1 3계층 컨텍스트 (토큰 절감)
매 턴 전체 대화를 재전송하지 않는다:
1. **정적 로어** — 시스템 지시에 **1회만**(`WORLD_LORE`).
2. **롤링 요약** — 모델이 매 턴 `updated_summary`를 스스로 갱신(오래된 턴을 압축).
3. **최근 N턴**(6턴)만 원문 유지(`MAX_RECENT_TURNS`).

씬 앵커도 **활성 노드 것만** 주입(`sceneAnchor`) — 전체 씬 바이블을 보내지 않음.

### 4.2 출력 3중 방어선
소형/클라우드 모델의 형식 붕괴를 **구조로** 막는다:
1. `responseSchema` — JSON 강제(스키마 제약).
2. `safeParse` — 관대한 파싱(마크다운 펜스 제거 등).
3. `normalize` — enum 폴백, 수치 클램프, **한자/CJK 문장부호 제거**, 선택지 길이컷,
   노드 화이트리스트 검증(환각 노드 거부).

### 4.3 하이브리드 프로바이더 라우터 (`aiRouter.js`)
우선순위 **BYOK 클라우드 > 로컬 자체모델 > 스크립트 데모**. 클라우드가 일시적으로
막히면(무료 티어 한도 등) **로컬로 자동 폴백**. "내 모델 전용" 모드(키 있어도 로컬만 —
오프라인/프라이버시)도 지원. 순수 함수로 분리해 유닛 테스트.

---

## 5. 자체 모델 증류 파이프라인 (`ml/`)

상업 배포엔 BYOK(키 요구)도 서버 호스팅(1인당 과금)도 부적합 → **로컬 모델 번들**이 답.

- **교사 → 학생 증류**: 게임이 클라우드에 보내는 것과 **동일한 입력**으로 교사(Claude
  서브에이전트 병렬 생성)를 돌려 `(입력 → 정제 JSON)` 학습쌍 600개를 모으고, 소형
  Qwen2.5-7B가 같은 형식을 배우게 QLoRA 파인튜닝.
- **데이터 품질 설계**: NPC 4종 균형, 증거 HIT/MISS 케이스, 짧은 1인칭 선택지, 순한글 강제.
- **평가 자동화**: 홀드아웃 40개로 파싱·enum·노드·한국어·선택지 준수율 측정(`eval.mjs`),
  모델 간 A/B(`compare-models.mjs`).

### 학습 인프라 결정 (ADR 후보)
- Colab(무료 T4)은 **런타임 끊김·저장 실패**로 학습 결과를 두 번 유실 → **맥 로컬 MLX**로
  전환(Apple M5/24GB). 끊김·비용 0.
- 24GB 통합메모리는 OS와 공유 → **batch 2 + grad-checkpoint**가 안전선(Peak ~11GB).
  배치를 키우고 체크포인트를 끄면 스왑 폭주로 시스템 다운(실측).
- `huggingface_hub` 다운로더가 대형 파일에서 멈추는 문제 → **curl 직접 다운로드**로 우회.

> 상세 삽질 기록은 `DEVLOG.md`, 파이프라인 가이드는 `ml/README.md`.

---

## 6. 시네마틱 (수미상관)

인트로: AI 시네마틱 영상 → CRT/글리치 오버레이로 게임 룩과 통일 → 글리치 타이틀 → 잭인.
아웃트로: 세션 종료·봉인 → 결말별 세계 결말 → 엔딩 카드. 한글 코드레인·CRT로 통일.
연출 자산은 CSS/Canvas 중심(외부 에셋 최소)이라 라이선스·워터마크 부담이 없다.

---

## 7. 엔지니어링 신뢰도

- **vitest 유닛 테스트 66개** — 상태머신(`applyResponse`·게이팅·추적·실패·클램핑),
  엔딩 게이트, 출력 방어선, 하이브리드 폴백, 오프라인 데모, 페이싱 추정.
- **콘텐츠 그래프 검증기**(`scripts/validate-content.mjs`) — BFS 도달성·막다른길·
  화이트리스트·enum. 노드 19개 전부 통과.
- **CI** 연결(푸시 시 테스트 가동).

---

## 8. 열린 과제 (다음 반복)

- **서사 심화(Phase 2)** — `voices.js`/`backstories.js`를 저자 문체로 리라이팅(원본성).
- **밸런싱** — 실제 플레이 로그로 임계값(호감/의심/추적) 튜닝, 표로 기록.
- **자체 모델 품질** — 로컬 재학습본이 Colab본(준수율 88.6%)을 넘도록 학습 설정 강화
  (전 층 LoRA·에폭·rank).
