# 📓 DEVLOG — Aetheria 2099

AI 기반 인터랙티브 픽션의 개발 일지. 무엇을 만들고, 왜 그렇게 결정했고,
무엇에서 막히고 어떻게 풀었는지를 시간순으로 남긴다. (최신 항목은 맨 아래에 추가)

---

## 2026-08-09 — 프로젝트 시작: 프로토타입 구축

### 기획 검수 → 착수
- 명세서(BYOK Gemini 기반, 운영비 0원, Web MVP→상업화)를 서사·비용·아키텍처
  세 관점에서 먼저 검토. 코드보다 설계 판단을 앞세움.
- 스택 확정: **Vite + React + Tailwind + lucide** (BYOK 클라이언트 직접 호출 →
  SSR/localStorage 충돌 없음, Vercel·Electron 양쪽 대응).

### 핵심 아키텍처 결정
- **AI 출력을 JSON으로 강제**: Gemini `responseSchema` + enum + 노드 화이트리스트
  + 3단 파서 폴백. "할루시네이션으로 포맷이 깨지는 것"을 구조로 방어.
- **상태머신 분리**: NPC별 게이지(호감/의심), trade-off, 게이팅, 버전드 세이브
  (SaveGameV1) — 웹 LocalStorage ↔ 데스크톱 JSON 파일 동일 직렬화.
- **토큰 절감 3계층 컨텍스트**: 정적 로어(시스템 인스트럭션 1회) + 롤링 요약
  (모델이 `updated_summary`로 자체 갱신) + 최근 N턴만 verbatim.
- **비용 방어**: 429/에러 시 "NEXUS 회선 과부하" 비상 모드 + 지수 백오프.

### 만든 것
- 게임 루프(대화·나레이션·선택지·자유입력), 네온 HUD, 글리치/CRT/절차적 사운드.
- 씬 바이블(14→17노드): 노드별 앵커(setting/goal/beats/dilemma) + beatBudget
  페이싱 + 플레이타임 추정.
- 엔딩 시퀀스, 오프라인 데모 모드(키 없이 플레이).

### 막힌 것 → 푼 것 (기억할 것)
- **모델 키 이슈**: 실플레이에서 `gemini-2.0-flash`가 `429 limit:0`(무료 티어 0).
  진단으로 `gemini-2.5-flash`는 되지만 `thinkingConfig`가 특정 키에서 `400`.
  → **`gemini-flash-latest`(롤링 별칭) + thinkingConfig 제거 + maxOutputTokens 상향**
  으로 여러 키에서 안정화. (모델 ID는 상수 한 곳에서 관리.)
- **한자 혼입**: 모델이 "압류(押留)"처럼 한자를 섞음. → 프롬프트 "순수 한글"
  지시 + 클라이언트 정규식 필터(이중 방어). 리터럴 한자 범위가 저장 시 변형돼
  한글까지 지우는 함정 → **`\u` 이스케이프 ASCII 정규식**으로 교체.

### 컴공 아티팩트
- **콘텐츠 그래프 검증기**(`scripts/validate-content.mjs`): 스토리를 방향 그래프로
  보고 참조 무결성·BFS 도달 가능성·막다른 길·엔딩 도달성·화이트리스트·enum을 검사.
  깨진 간선 주입 테스트로 실제 검출 확인. `npm run validate` + CI 워크플로.

---

## 2026-08-10 — 캐릭터·서사 시스템 심화, 그리고 방향 재설정

### 캐릭터 목소리·백스토리 (원본 집필)
- 4인(렌·카엘·에코·NEXUS) **목소리(voices.js)** 를 직접 집필한 예시 대사로 규정.
  실AI 테스트에서 렌이 "공짜는 없어…" 등 심은 문체를 그대로 재현함을 확인.
- **백스토리(backstories.js)**: 각 NPC의 과거·동기·비밀. 무대 NPC 것만 주입(토큰 절약).
- 주인공 **제인의 개인 서사**: 지워진 3년(빈자리 조각 #1~#4), 자장가 = NEXUS 통제
  문구와 동일하다는 복선. 세계의 진실과 개인의 진실을 Act3에서 합류.

### 서사 시스템 3종
- **월드 스테이트(flags) + 이야기 일지**: AI가 확정한 사실을 영구 기록 → 모순 방지·콜백.
- **NEXUS 추적도(heat)**: 전역 감시 시계. 눈에 띄는 행동↑/잠행↓, 100=드론 급습(2번째 실패 경로).
- **결정론적 엔딩 게이트**: `eligibleEndings(save)` — 호감/의심/추적/조각으로 "열리는
  엔딩"을 코드가 계산, AI는 그중에서만 선택. 엔딩 4→**6개**로 확장(개인 진엔딩 포함).
  실AI로 에코 동맹→부서진 요람, 빈자리4개→되찾은 이름 수렴 확인.

### 연출
- **인트로/엔딩 시네마틱**(수미상관): 잭인 부팅 시퀀스 ↔ 잭아웃 종료 로그 + 글리치 타이틀.
- 한글 **픽셀 폰트(Galmuri)** 인트로 적용.
- (기록) 풀스크린 반응형 배경 애니는 **접음** — HUD가 화면을 덮어 배경이 안 보임.
  교훈: 꽉 찬 HUD엔 풀스크린 배경보다 "독립된 풀스크린 순간(인트로/엔딩)"이 맞음.

### 방향 재설정
- "하루 만에 AI로 만든 것"의 얕음을 경계. **깊이는 기능이 아니라 (a)원본 창작
  (b)문서화된 판단 (c)반복 튜닝의 궤적**에서 나온다고 정리 → 5~6주 심화 로드맵 수립
  (`ROADMAP.md`). Phase 2(서사 심화, 내 손)부터 착수.

### Phase 2 착수 — 카엘 루트 (원본 집필)
- 카엘 루트 두 씬(취조실 → 원칙의 균열)을 **내 문체로 집필**.
- 새 인물 **아렌** 창조: 20년 전 카엘이 놓아준 진실 추적자(사살당함). 카엘의
  "이름 없던 비밀"에 이름·무게·게임 기능을 동시에 부여.
- 통합 결정: **아렌의 인식표 = keyEvidence**(씬1에서 코덱스 자동 해금 → 씬2에서
  제시 시 카엘 균열), **바이패스 키 = world flag**(`kael_gave_bypass_key`),
  백스토리 캐논에 아렌 정식 반영. 서사와 시스템이 맞물림.
- validate·build 통과.

### Phase 2 — 렌 루트 (원본 집필)
- 렌 루트 두 씬(옥션 → 낙찰의 대가)을 **내 문체로 집필**. 지하 폐공장 경매,
  총격전 난입, 이자 논리의 도주/배신 분기.
- 카엘의 '아렌'과 대칭으로, 렌의 백스토리 비밀(**미매각 칩 #00-X** — 사랑했던
  이의 마지막 미소)을 keyEvidence로 승격. 씬1 자동 해금 → 씬2 제시.
- 소문 유출을 world flags(`chip00_leaked`, `outside_rumor_spreading`)로 기록.
- **삼대립 완성**: 세 루트 모두 "NPC의 가장 아픈 비밀을 증거로 들이대 균열"이라는
  공통 구조 + 각자 다른 결(자유/질서/자본). validate·build 통과.

### Phase 2 — Act 3 & 엔딩 (원본 집필)
- 클라이맥스 두 씬(요람으로 가는 길 → 요람의 역설) + 엔딩 6종을 **내 문체로 집필**.
- **빈자리 #4 반전**: 제인 = 리엔의 수석 연구원, 정화된 하늘을 본 뒤 스스로 기억을
  지운 피험자. 프롤로그의 "너였구나"·자장가·초록 잔상을 전부 회수 → 개인 서사 완결.
- 새 요소: 제인 내면 독백(나레이션), 자장가 = 리엔의 인코딩 음성(빈자리 #3 갱신),
  리엔의 "감옥이 아니라 울타리" 논리. 엔딩별 명대사 6종.
- 미해결: 연도 표기 50 vs 90 통일 필요(캐논 체크). validate·build 통과.

### Phase 2 — 캐논 정리 & 실플레이 검증
- 연도 **50년으로 통일**(최종 대사의 90→50).
- **실AI 검증 (내 원본 서사가 의도대로 작동하는지)**:
  - 카엘 루트: 아렌 인식표 제시 → 카엘 균열 대사 거의 그대로 재현, 호감 55→65,
    바이패스 키 선택지 등장. ✅
  - Act 3 클라이맥스: "정체를 돌려달라" → 리엔이 빈자리 #4(수석 연구원 반전)를
    고백, 자장가 모티프 회수, 조각 3→4. ✅
- 결론: 증거 메커니즘·상태 시스템·개인 서사 반전이 유기적으로 맞물려 실제 플레이됨.

### Phase 1(마감) — 배포 준비
- 소소한 마감 3종: 진단 로그 제거 / 자유입력 **Enter 전송 IME 대응**
  (조합 중 Enter 무시) / **인트로 1회 재생 후 스킵**(localStorage 플래그).
- git 저장소 초기화(main), 첫 커밋, GitHub 연동(kim-taek-jin/aetheria-2099).
- 막힘→해결: PAT에 `workflow` 스코프가 없어 `.github/workflows/ci.yml` 푸시 거부
  → 워크플로만 추적 제외하고 푸시(라이브 우선). CI는 스코프 추가 후 후속 커밋 예정.

### 🚀 배포 완료 — 라이브
- Vercel(Hobby/무료)로 배포. **https://aetheria-2099.vercel.app**
- Framework 자동감지(Vite), 환경변수 0(BYOK). 프로덕션에서 인트로·오프라인 데모
  정상 작동 확인. 이후 main 푸시 시 자동 재배포.
- 마일스톤: "만든 것" → 남에게 링크로 보여줄 수 있는 "포트폴리오"가 됨.

### 자체 모델 착수 — 증류 파이프라인 (스팀/데스크톱 대비)
- 목표: Gemini를 소형 모델(Qwen2.5-3B)로 **증류(distillation)** → 파인튜닝하여
  스팀/데스크톱 빌드에서 **API·키 없이 오프라인·운영비 0** 구동. (웹은 BYOK/데모 유지)
- 왜 자체 모델: 상업 게임에 BYOK(키 요구)·서버 호스팅(1인당 과금) 모두 부적합 →
  로컬 모델 번들이 정답. 3B로 작게 만들면 고객 PC 범위↑.
- 구현: `ml/generate-dataset.mjs`(게임과 동일 입력으로 Gemini 돌려 학습쌍 수집,
  이어하기 지원), `ml/finetune_colab.py`(Unsloth QLoRA, T4), `ml/README.md`.
  최적화: 학습 레코드는 **짧은 system**(규칙은 가중치로 학습, T4 토큰 절감).
- geminiService에서 systemInstruction/buildContents export(파이프라인 재사용).

---

## 2026-08-12 — 증류 교사 전환, 테스트/CI, 결말 수집

### 자체 모델 — Claude 교사 + 파일럿 데이터셋
- 증류 교사를 `PROVIDER`로 선택하게 개편(`ml/generate-dataset.mjs`): Gemini 무료 ↔
  **Claude(Anthropic Messages API)**. Claude 경로는 **전체 systemInstruction을
  프롬프트 캐싱**(입력비 ~90%↓)하고 **tool-use로 스키마 강제**, 저장 레코드의
  system은 SHORT_SYSTEM 유지(학습/추론 단일 출처).
- Haiku 4.5로 **324개** 생성. 전수 검사: 파싱 324/324, **한자 0·노드무효 0·
  선택지≠3 0·enum무효 0**. (Gemini 무료에서 잦던 한자·포맷깨짐이 사라짐.)
- 학습/평가 분리: 셔플 후 **train 285 / eval(홀드아웃) 40** — 정직한 baseline용.
- Colab `.ipynb` 생성(T4·3에폭·TRL 인자 위치 수정), 마지막 셀 **GGUF → Google
  Drive 자동 저장**. `ml/Modelfile`(SHORT_SYSTEM SYSTEM + temp 0.9)로 Ollama 등록.

### 컴공 아티팩트 — 유닛 테스트 + CI (Phase 3)
- **vitest** 도입. 순수 로직 **44 테스트**: `state.js`(게이지 클램프·trade-off
  파급·heat baseline·페이싱·조각 중복제거·플래그 병합·**두 갈래 실패**·엔딩·
  게이팅·직렬화), `geminiService`(safeParse·normalize 방어선·한자 제거),
  `scenes`(결정론적 엔딩 게이트), `collection`(수집 병합).
- CI(`ci.yml`)에 `npm test` 단계 추가 — validate → **test** → build.

### 재미 레버 — 결말 수집(재플레이 훅)
- `src/game/collection.js`: 세이브와 분리된 LocalStorage에 **발견한 엔딩**을
  영구 기록(순수 `mergeDiscovered`로 테스트 가능). 결말 화면에 **"수집 N/6" +
  시길 갤러리 + NEW 배지 + 전부 발견 시 축하 문구**. 미발견은 ◇로 스포일러 방지.
- 핵심 루프 무수정의 **순수 추가**라 라이브 회귀 위험 0. validate·test·build 통과.

### 자체 모델 — 학습 완료 → 게임 연결 (증류 파이프라인 완결)
- Colab(T4) 학습: 로스 **1.89→0.93** 단조 감소, 3에폭 108스텝. 셀7 출력에서
  **원본 자장가 대사·캐논까지 재현** 확인.
- Ollama 등록(`aetheria`, q4_k_m 1.8GB) 후 **홀드아웃 40개 eval: 종합 94.6%**
  (파싱·enum·노드 100%). 유일 약점 한자 80% → 게임 `normalize`의 `stripHanja`가
  런타임에 제거하므로 실사용 한국어율 ~100%(방어선으로 이미 커버).
- **프로바이더 배선**(`App.jsx`): 우선순위 **BYOK 키 > 로컬 자체모델 > 스크립트
  데모**. 키 없이 로컬 Ollama가 떠 있으면 `ollamaProvider`로 풀 AI 구동(오프라인·
  운영비 0). 감지는 `isAvailable()` 1회, 실패 시 조용히 데모로 강등.
- **실기기 검증**: dev 서버 + 인앱 브라우저에서 키 없이 "🖥 로컬 자체 모델" 배지
  확인, 자유 입력 한 턴이 로컬 3B로 실제 생성됨(콘솔 에러 0). "스팀용 자체 모델"
  서사가 코드 끝단까지 연결됨.

### 방향 재설정 — 하이브리드 + 모델 상향(3B→7B)
- 실플레이 피드백: 로컬 3B 품질이 Gemini의 절반 이하. **증류는 형식은 옮겨도
  100배 큰 교사의 추론·유창함은 못 옮긴다**(모델 크기의 한계, 데이터로 못 메움).
- 상업화 재검토: "서버 호스팅=상업 불가" 전제가 바운디드 서사 게임엔 과함 —
  1회차 ~25턴 × 캐싱 시 턴당 ~$0.005 ≈ **회차당 ~$0.12**, $12 게임에서 5% 미만.
  → 호스팅/BYOK도 경제성 있음. 하지만 이번엔 **하이브리드**로 결정.
- **하이브리드 확정**: `App.jsx` 우선순위 `BYOK 키 > 로컬 자체모델 > 데모` = 키 있으면
  Gemini(HD)·없으면 로컬(오프라인·$0)·둘 다 없으면 스크립트. 배지도 HD 안내로 갱신.
- **모델 상향 3B→7B**: Colab 노트북·`.py`·Modelfile을 **Qwen2.5-7B-Instruct**로 교체
  (한국어·일관성↑, T4 QLoRA 가능·느림·gguf ~4.5GB). Ollama 태그 `aetheria` 유지.
- 선택지 명세 강화(짧게·제인 시점) + normalize 길이컷 + 로컬 온도 0.9→0.65.
  명세 조인 건 **BYOK/Gemini 경로엔 즉시** 반영(웹 라이브 개선), 로컬은 7B 재학습으로 반영.

### 하이브리드 복원력 — 프로바이더 폴백(클라우드→로컬)
- 무료 티어 rate limit을 세션 내내 겪음 → **BYOK(Gemini)가 일시적으로 막히면
  로컬 자체모델로 자동 전환**해 플레이가 안 끊기게. (기존엔 "NEXUS 회선 과부하"로 끊김)
- 라우팅을 순수 모듈 `src/services/aiRouter.js`로 분리(`routeBeat`, `isTransient`):
  키 있으면 cloud, 없으면 local, cloud 일시 실패+로컬 가용 시 `local-fallback`.
  BAD_KEY/NO_KEY는 폴백 안 함(키 수정 유도). 프로바이더 함수 주입 → 유닛 테스트 가능.
- `App.jsx` advance()가 `routeBeat` 사용, 폴백 턴엔 앰버 배너(☁→🖥) 표시.
- 테스트 **7개 추가(총 52)**, build 통과, 실기기(로컬 경로) 한 턴 검증.
- 데이터 재생성기 개선: **연속 RATE_LIMIT 15회면 일일한도로 판단·자동 종료**
  (백그라운드 무한 60초 폴링 방지, append/resume). `ml/split-dataset.mjs` +
  `npm run ml:split`(seed 고정 train/eval 분할). 7B 학습은 기존 285개로 진행 중.

### "내 모델 전용" 모드 (로컬 온리)
- 요청: 키가 있어도 **클라우드 안 쓰고 자체 모델로만** 플레이하는 모드(오프라인·
  프라이버시·스팀 배포용). 기존엔 키 있으면 무조건 클라우드라 강제 불가였음.
- 헤더에 **Cpu 토글**(켜짐=초록). 켜면 `forceLocal` 저장 + Ollama 즉시 재감지.
  라우팅에 `effKey = forceLocal ? '' : apiKey` 를 넘겨 클라우드를 건너뜀.
- 상태별 안내: 로컬 전용 구동 중(초록 배지) / 로컬 전용인데 Ollama 미실행이면
  앰버 배너 + **재감지 버튼**(모드 유지한 채 가용성만 다시 확인). aiReady도 모드에
  맞게 계산(전용이면 ollama 필수).
- 실기기 검증: **더미 키가 있는데도** "내 모델 전용" 배지 + 한 턴이 **로컬로 생성**
  (비상 비트 아님) → 클라우드 미사용 확인. build·test 52 통과, 콘솔 에러 0.

### 게임 완성도 폴리시 3종 (선택의 무게 · 가이드 · 온보딩)
- **① 선택의 무게(피드백 연출)**: 매 턴 상태 변화를 눈에 보이게 — 활성 NPC의
  의심/호감 + NEXUS 추적 **델타를 게이지 위에 플로팅**(+3/-2, 좋음=초록·나쁨=빨강,
  변화 0이면 미표시). **증거 HIT/MISS는 화면 전체 플래시**("◆ 증거 적중" / "✕ 빗나감").
  App에 `computeDelta` 순수 헬퍼, StatusPanel/Gauge에 delta prop, index.css 키프레임.
- **② 진행/엔딩 가이드**: 상태패널 아래 얇은 라인 — 우세 세력(호감 최고, ≥30일 때)
  + 결말 노드 도달 시 "◈ 결말 분기 임박". 방향 상실 방지, 스포일러 없음.
- **③ 첫 플레이 온보딩**: `TutorialOverlay` — 관계 게이지·NEXUS 추적·증거 3규칙을
  인트로 후 1회 브리핑(localStorage 기억). 신규 진입장벽↓.
- 실기기 검증: 온보딩 표시(스크린샷) → 닫고 한 턴 → `.delta-float`에 "+3" 렌더 확인,
  콘솔 에러 0. build·test 52 통과. 핵심 루프 무수정·additive.

### 30초 시네마틱 인트로 (인게임 확장)
- AI 영상툴 대신 **엔진으로 직접** — 외부 에셋 0·워터마크 없음·게임 룩 100% 일치.
- `IntroSequence`를 6장면 상태머신으로 확장(~30초, 스킵 가능):
  부팅 로그 → 세계(돔·NEXUS의 눈·스카이라인·비) → 감옥(몽유 시민·자장가 자막) →
  제인(후드 실루엣·뉴럴잭·칩 #00) → **초록 유출**(SIGNAL LEAK·햇살 방사, 훅) →
  삼대립(렌 앰버·카엘 시안·에코 마젠타 실루엣) → 글리치 타이틀+잭인 버튼.
- 전부 CSS 키프레임(rain/nexusPulse/leakBurst/figRise/cineIn)·진행점·스킵 힌트.
- 실기기 검증: 감옥·유출·타이틀 장면 스크린샷 확인, 잭인→게임 인계, 콘솔 에러 0.
  build·test 52 통과.
- 데이터 생성: 2키 로테이션 정상(키#1 한도→키#2 전환 확인)이나 둘 다 오늘 quota
  소진 → **20개에서 자동 종료**(신규 5). 내일 이어서.

### 인트로 재설계 (2차 — 확정본)
- 1차(도형 실루엣)·2차(포스터형 대형 자막) 모두 "AI가 만든 듯·짜침" 피드백.
  교훈: **CSS로 사람/장면을 그리거나, 큰 글자를 화면 중앙에 띄우면 싸구려로 보인다.**
- 확정: 세계관을 **떠다니는 글자가 아니라 게임 안 물건(NEXUS 기밀 파일 dossier)** 으로
  읽힌다. `IntroSequence` = 복호화 터미널 → 세계관 4줄(기록/봉쇄/지성/주체) →
  **초록 유출**(공식 기록을 데이터로 뒤집음: 실측·판정 "기록 위조 확인", 큰 글자 없음)
  → 글리치 타이틀. 한글 자모 코드레인(canvas)·비네트·CRT로 무드. ~16초+타이틀, 스킵.
- 사용자 확정("완벽해"). build·test 52 통과, 콘솔 에러 0.

### 아웃트로 — 잭아웃 시네마틱 (인트로와 수미상관)
- 엔딩 도달 시 EndingScreen 앞단에 **잭아웃 터미널** 삽입: "NEXUS 기억 파일 #00 ·
  세션 종료 · 봉인 100%" + 잭아웃 로그 + **결말별 [결말] 세계 결말 1줄**(6종) →
  4.6초 후 기존 엔딩 카드(시길·명대사·통계·수집)로. 인트로의 복호화와 짝.
- CodeRain을 공용 컴포넌트(`CodeRain.jsx`)로 추출해 인트로/아웃트로가 공유.
- 실기기 검증(세이브 주입으로 엔딩 강제): 잭아웃 터미널·엔딩 카드 정상. build·test 52.

<!-- 다음 작업 세션마다 아래에 항목 추가:
## YYYY-MM-DD — 제목
### 무엇을
### 왜 (결정/근거)
### 막힌 것 → 푼 것
-->
